レベル <%- level.number %>

<%- level.description %>

ヒント: <%- level.tip %>


<%- level.floorMap %>

  > = stairs
<% Object.keys(level.units).forEach(function (name) { -%>
  <%= level.units[name] %> = <%= name %>
<% }); -%>


アビリティ:
<% level.warriorAbilities.existent.forEach(function (ability) { -%>

  warrior.<%- ability.name %>()
<% }); -%>
<% level.warriorAbilities.new.forEach(function (ability) { -%>

  warrior.<%- ability.name %>() (NEW!)
<% }); -%>


Player.js の編集が完了したら、再度 warriorjs-ja コマンドを実行してください。
